{
  "name": "yojana-sarthi-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Convenience root scripts - the real app lives in ./backend",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "seed": "npm --prefix backend run seed"
  }
}
